{
  "name": "member-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node tests/sync-engine-conflict.test.mjs",
    "preview": "npm run build && serve dist -l 4173"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
